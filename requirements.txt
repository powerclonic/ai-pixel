# Dependências mínimas do aplicativo Pixel Place
# Versões não rigidamente fixadas para facilitar updates de segurança no deploy.
# Se quiser builds totalmente reprodutíveis, fixe versões exatas (ex: fastapi==0.111.0, uvicorn[standard]==0.30.1)

fastapi
uvicorn[standard]
